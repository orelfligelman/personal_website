<h1>Listing tips</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tips.each do |tip| %>
      <tr>
        <td><%= link_to 'Show', tip %></td>
        <td><%= link_to 'Edit', edit_tip_path(tip) %></td>
        <td><%= link_to 'Destroy', tip, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tip', new_tip_path %>

 <div class="example" id="customizing-balloon">
    <h3>Hover over me for some life advice</h3>
    <div class="ie-fix"><div id="example6" title="Because its not good for you" class="target">Why shouldn't you smoke crack?</div></div>
    <div class="desc">
    <p>Hover over me for some advice.</p>
    <div class="source">
      <div class="js">$('#example6').bt({
  padding: 20,
  width: 100,
  spikeLength: 40,
  spikeGirth: 40,
  cornerRadius: 40,
  fill: 'rgba(0, 0, 0, .8)',
  strokeWidth: 3,
  strokeStyle: '#CC0',
  cssStyles: {color: '#FFF', fontWeight: 'bold'}
});</div>
   </div>
    </div>
    </div>

    